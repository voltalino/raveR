% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composer.R
\name{raver_compose}
\alias{raver_compose}
\title{Compose a complete track from code analysis}
\usage{
raver_compose(
  code_model,
  bpm = NULL,
  seed = NULL,
  sample_rate = SAMPLE_RATE,
  genre = "deep_house"
)
}
\arguments{
\item{code_model}{CodeModel object from raver_analyze()}

\item{bpm}{Numeric tempo. If NULL, auto-selects based on file hash
(always within 118-124 range). Default NULL.}

\item{seed}{Random seed for reproducibility. If NULL, uses file_hash
for determinism. Default NULL.}

\item{sample_rate}{Integer sample rate in Hz. Default SAMPLE_RATE.}

\item{genre}{Character genre name. One of "deep_house", "techno", "ambient",
"drum_bass", or "house". Default "deep_house".}
}
\value{
A Wave object containing the complete track
}
\description{
Main composition function that generates a complete deep house
track from a CodeModel. Integrates all components (instruments, sequencer,
arrangement, motifs) into a coherent track.
}
\details{
The composition process:
\enumerate{
\item Set random seed for determinism
\item Auto-select or validate BPM
\item Create arrangement from code model
\item Generate each section
\item Concatenate sections
\item Apply master processing (normalize, limit)
}
}
\examples{
\dontrun{
model <- raver_analyze("my_script.R")
track <- raver_compose(model, bpm = 122)
raver_export_wav(track, "output.wav")
}

}
