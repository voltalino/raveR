% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bass.R
\name{raver_bass_octave_pattern}
\alias{raver_bass_octave_pattern}
\title{Create classic house octave bass pattern}
\usage{
raver_bass_octave_pattern(
  root_midi,
  pattern,
  bpm,
  velocity = 0.9,
  cutoff_hz = 800
)
}
\arguments{
\item{root_midi}{MIDI note number for the root note (bass range recommended)}

\item{pattern}{Character vector specifying the pattern. Valid values:
"root" (plays root note), "octave" (plays root + 12), "rest" (silence)}

\item{bpm}{Beats per minute (tempo)}

\item{velocity}{Velocity from 0.0 to 1.0. Default 0.9.}

\item{cutoff_hz}{Lowpass filter cutoff. Default 800.}
}
\value{
A Wave object containing the bass pattern
}
\description{
Creates the classic house bass pattern with root note and
octave jumps. This pattern is fundamental to deep house.
}
\details{
Each element of the pattern is a 16th note. A typical house bass uses
patterns like c("root", "rest", "octave", "rest") repeated.
}
\examples{
\dontrun{
# Classic octave pattern on C2
pattern <- c("root", "rest", "octave", "rest")
bass <- raver_bass_octave_pattern(36, rep(pattern, 4), bpm = 120)
}

}
