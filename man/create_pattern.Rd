% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequencer.R
\name{create_pattern}
\alias{create_pattern}
\title{Create a pattern for the step sequencer}
\usage{
create_pattern(instrument, steps, note = NULL, velocity = 1, swing = 0)
}
\arguments{
\item{instrument}{Character string identifying the instrument type.
Valid values: "kick", "snare", "clap", "hihat_closed", "hihat_open",
"bass", "pad"}

\item{steps}{A 16 or 32 element vector. Can be:
\itemize{
\item Logical: TRUE = hit at full velocity, FALSE = rest
\item Numeric: 0-1 velocity per step (0 = rest)
}}

\item{note}{MIDI note number for melodic instruments (bass, pad).
Ignored for drums. Default NULL.}

\item{velocity}{Numeric base velocity multiplier from 0.0 to 1.0.
Applied to all steps. Default 1.0.}

\item{swing}{Numeric swing amount from 0.0 to 0.25 for this pattern.
Default 0.0.}
}
\value{
A pattern list with elements:
\itemize{
\item steps: Numeric vector of velocities (0-1) per step
\item instrument: Character instrument identifier
\item steps_per_bar: Integer (16 or 32)
\item note: MIDI note or NULL
\item velocity_base: Base velocity multiplier
\item swing: Swing amount
}
}
\description{
Creates a pattern data structure for one bar of a single
instrument. The pattern contains step triggers and timing information.
}
\examples{
\dontrun{
# Four-on-the-floor kick
kick <- create_pattern("kick",
  c(TRUE, FALSE, FALSE, FALSE, TRUE, FALSE, FALSE, FALSE,
    TRUE, FALSE, FALSE, FALSE, TRUE, FALSE, FALSE, FALSE))

# Hi-hats with swing
hihat <- create_pattern("hihat_closed", rep(TRUE, 16), swing = 0.08)

# Bass pattern with note
bass <- create_pattern("bass",
  c(TRUE, FALSE, FALSE, FALSE, FALSE, FALSE, TRUE, FALSE,
    FALSE, FALSE, FALSE, FALSE, TRUE, FALSE, FALSE, FALSE),
  note = 36)  # C2
}

}
