% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/playback.R
\name{wait_audio}
\alias{wait_audio}
\title{Wait for Audio Playback}
\usage{
wait_audio(instance = NULL)
}
\arguments{
\item{instance}{An audioInstance object from play_audio(). If NULL,
uses the most recently started playback.}
}
\value{
NULL (invisibly)
}
\description{
Blocks execution until the audio playback completes. If no instance
is provided, waits for the most recently started playback.
}
\examples{
\dontrun{
# Play and wait for completion
tone <- raver_sine(440, 2)
play_audio(tone)
wait_audio()  # Blocks for ~2 seconds

# Or with explicit instance
instance <- play_audio(tone)
wait_audio(instance)
}
}
