% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequencer.R
\name{render_pattern}
\alias{render_pattern}
\title{Render a pattern to audio}
\usage{
render_pattern(pattern, bpm, sample_rate = SAMPLE_RATE)
}
\arguments{
\item{pattern}{A pattern list created by create_pattern()}

\item{bpm}{Numeric beats per minute (tempo)}

\item{sample_rate}{Integer sample rate in Hz. Default SAMPLE_RATE.}
}
\value{
A Wave object containing one bar of the rendered pattern
}
\description{
Renders a pattern to a Wave object representing one bar of audio.
Applies swing timing and velocity scaling to each step.
}
\details{
The pattern is rendered step by step:
\enumerate{
\item Calculate the sample position for each step (with swing applied)
\item For each active step (velocity > 0), render the instrument hit
\item Mix all hits into the output buffer
\item Overlapping notes are summed (mixed), not replaced
}
}
\examples{
\dontrun{
# Create and render a kick pattern
kick_pat <- create_pattern("kick",
  c(TRUE, FALSE, FALSE, FALSE, TRUE, FALSE, FALSE, FALSE,
    TRUE, FALSE, FALSE, FALSE, TRUE, FALSE, FALSE, FALSE))
kick_wave <- render_pattern(kick_pat, bpm = 120)
}

}
