% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swing.R
\name{calculate_step_timing}
\alias{calculate_step_timing}
\title{Calculate exact timing for a step position}
\usage{
calculate_step_timing(step, bpm, swing_amount = 0, steps_per_bar = 16)
}
\arguments{
\item{step}{Integer step number (1-indexed: 1, 2, 3, ... up to steps_per_bar)}

\item{bpm}{Numeric beats per minute (tempo)}

\item{swing_amount}{Numeric swing amount from 0.0 to 0.25. Even steps are
delayed by this fraction of a step duration. Default 0.0.}

\item{steps_per_bar}{Integer steps per bar. Default 16 (16th notes).}
}
\value{
List with:
\itemize{
\item position_samples: Integer sample position from bar start
\item position_sec: Numeric seconds from bar start
}
}
\description{
Calculates the exact sample and time position for a step in a
pattern, applying swing to even-numbered steps using the Roger Linn algorithm.
}
\details{
The Roger Linn swing algorithm delays even-numbered steps while keeping
odd-numbered steps on the grid. The delay amount is swing_amount multiplied
by the step duration.

For example, with swing_amount = 0.10 at 120 BPM:
\itemize{
\item Step 1: 0 samples (on grid)
\item Step 2: base position + 10\% of step duration
\item Step 3: base position (on grid)
\item Step 4: base position + 10\% of step duration
}
}
\examples{
\dontrun{
# Step 1 timing at 120 BPM
t1 <- calculate_step_timing(1, 120)
# Step 2 with 10\% swing
t2 <- calculate_step_timing(2, 120, swing_amount = 0.10)
}

}
